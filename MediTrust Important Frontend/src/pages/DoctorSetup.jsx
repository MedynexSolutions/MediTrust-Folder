import RoleSetupPage from '@/components/setup/RoleSetupPage';

export default function DoctorSetup() {
  return (
    <RoleSetupPage
      role="doctor"
      title="Doctor setup"
      description="Create your doctor profile in MediTrust, then access your dashboard."
    />
  );
}
